#include <vector>

class Solution {
public:
    std::vector<int> twoSum(std::vector<int>& nums, int target) {
        // Deliberate out-of-range exception
        std::vector<int> emptyVec;
        return {emptyVec.at(100)}; 
    }
};